import { Grid } from "@mui/material"
import { FilterBar } from "../components"

export const DesignsPage: React.FC = () => {
    return (
        <Grid container>
            <FilterBar />
        </Grid>

    )
}
