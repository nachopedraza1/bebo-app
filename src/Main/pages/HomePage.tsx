import { LatestDesigns } from '../components';
import { Grid } from '@mui/material';

export const HomePage: React.FC = () => {

    return (
        <Grid container>
            <LatestDesigns />
        </Grid >
    )
}
